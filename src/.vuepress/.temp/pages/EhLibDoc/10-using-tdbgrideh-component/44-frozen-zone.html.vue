<template><div><h1 id="frozen-zone" tabindex="-1"><a class="header-anchor" href="#frozen-zone" aria-hidden="true">#</a> Frozen zone</h1>
<p>Frozen zone is columns the left part of the grid that remains unchanged when the grid is scrolled to the left or right direction.</p>
<p>Unlike the fixed columns the frozen columns can receive the edit focus. To specify the count of the un-scrolling columns you need to set <code v-pre>FrozenCols</code> property.</p>
</div></template>


