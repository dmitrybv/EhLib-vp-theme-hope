<template><div><h1 id="data-row-height" tabindex="-1"><a class="header-anchor" href="#data-row-height" aria-hidden="true">#</a> Data row height</h1>
<p>Use <code v-pre>RowHeight</code> and <code v-pre>RowLines</code> properties to specify the height of data row. Full height of data row is calculated as height in text lines of <code v-pre>RowLines</code> plus <code v-pre>RowHeight</code>. Set <code v-pre>RowSizingAllowed</code> to <code v-pre>True</code> value so that user can change height of row using mouse at run time.</p>
<p>Set <code v-pre>Column.WordWrap</code> to <code v-pre>True</code> value to break a long text in data row to a multiline. In reality, text will be wrapped only when height of row is bigger than height of one text line.</p>
<h3 id="individual-height-of-each-data-row" tabindex="-1"><a class="header-anchor" href="#individual-height-of-each-data-row" aria-hidden="true">#</a> Individual height of each data row.</h3>
<p><code v-pre>TDBGridEh</code> can work in mode when the height of every row is calculated dynamically depending on the text in the grid cells or size of image in the cell (When DrawGraphicData value is True). Include <code v-pre>dghAutoFitRowHeight</code> in <code v-pre>OptionsEh</code> property to force grid to calculate height for every row. This mode works only when the grid is connected to the <code v-pre>DataSet</code> of <code v-pre>TMemTableEh</code> type. When <code v-pre>dghAutoFitRowHeight</code> is included, grid calculates the height of the row dynamically using cell text (<code v-pre>TColumnEh.WordWrap</code> property also influence it) and for column the fields of which have <code v-pre>ftGraphic</code> type. To avoid surplus recalculation, grid recalculates heights, but not  for after every change of properties. For instance, grid does not update heights if the <code v-pre>Font</code> property was changed. In such cases it is necessary to call <code v-pre>UpdateAllDataRowHeights</code> method. For more flexible calculation of the heights the grid has an <code v-pre>OnGetRowHeight</code> event, in which you can specify the height of row. During the event calling, the <code v-pre>MemTableEh</code> positions on record and we can calculate the height of this record, so you can use <code v-pre>MemTableEh</code> properties to access fields values.</p>
</div></template>


