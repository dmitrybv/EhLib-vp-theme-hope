import{_ as e}from"./_plugin-vue_export-helper.cdc0426e.js";import{o,c as t,f as a}from"./app.ac77cf73.js";const d={},r=a('<h1 id="master-detail-relations" tabindex="-1"><a class="header-anchor" href="#master-detail-relations" aria-hidden="true">#</a> Master/detail relations</h1><p><code>Master/detail</code> relation is a working mode of two datasets when all visible records of one dataset (DetailDataSet) always correspond to one record of other dataset (MasterDataSet). In time of moving in <code>MasterDataSet</code> the <code>DetailDataSet</code> will recreate or filters records so that they will correspond to <code>MasterDataSet</code> record. The correspondence is adjusted using <code>MasterFields</code> and <code>KeyFields</code> properties of <code>DetailDataSet</code>. <code>DetailDataSet</code> always show only the records, which values of the field/fields defined by <code>MasterFields</code> property is equal to values of the fields defined by <code>MasterFields</code> property of the current record of <code>MasterDataSet</code>.</p><p><code>TMemTableEh</code> allows to create a master/detail relations on the client side &quot;mdsOnSelfEh&quot; (by filtering record) or on DataProvider &quot;mdsOnProviderEh&quot; (by recreating list of records in DetailDataSet when record in MasterDataset is changed).</p><p>To adjust <code>TMemTableEh</code> in the master/detail mode use next properties: <code>MasterSource</code>, <code>MasterFields</code>, <code>KeyFields</code>, <code>MasterDetailSide</code>.</p><p>When <code>MasterDetailSide</code> is <code>mdsOnSelfEh</code>, <code>TMemTableEh</code> will filter records by <code>DetailFields</code> fields using values of <code>MasterDataSet</code>[MasterFields] fields. When <code>MasterDetailSide</code> is <code>mdsOnProviderEh</code>, <code>DetailDataSet</code> will reopen itself, assigning parameters from <code>MasterDataSet</code> [MasterFields]. On opening it will pass parameters to <code>DataDriver</code>. In the <code>mdsOnProviderEh</code> mode the <code>DetailFields</code> are not used.</p><p>The third state of working &quot;mdsOnSelfAfterProviderEh&quot; is a combination of two preceding. Upon first moving to record in <code>MasterDataSet</code> the <code>DetailDataSet</code> gets records from <code>DataDriver</code>, sending him parameters with values, assigned from MasterSource[MasterFields] and add received records in its internal array of record. Upon next moving on same record in <code>MasterDataSet</code> the <code>DetailDataSet</code> will always only filter records locally.</p>',6),s=[r];function i(c,l){return o(),t("div",null,s)}const f=e(d,[["render",i],["__file","04-master-detail-relations.html.vue"]]);export{f as default};
