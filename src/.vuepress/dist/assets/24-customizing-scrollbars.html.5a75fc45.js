import{_ as e}from"./_plugin-vue_export-helper.cdc0426e.js";import{o,c as t,f as r}from"./app.ac77cf73.js";const c={},d=r('<h1 id="customizing-scrollbars" tabindex="-1"><a class="header-anchor" href="#customizing-scrollbars" aria-hidden="true">#</a> Customizing scrollbars</h1><p><code>TDBGridEh</code> and vertical scrollbar. If you work with different types of dataset you can notice that for some types of dataset the DBGrid shows vertical scrollbar correctly, but for other dataset their vertical scrollbar has only three positions independently of record count in dataset. To adjust the vertical scrollbar accommodation the <code>DBGrid</code> uses <code>RecordCount</code> and <code>RecNo</code> property of <code>DataSet</code> component. But some dataset holds <code>-1</code> in <code>RecordCount</code> and <code>RecNo.DataSet</code> function <code>IsSequenced</code> indicates whether the underlying database table uses record numbers to indicate the order of records. When <code>IsSequenced</code> returns <code>True</code> value, applications can safely use the <code>RecNo</code> property to navigate through records in the dataset and <code>DBGrid</code> can use <code>RecNo</code> property to show thumb position in vertical scrollbar. But when <code>IsSequenced</code> returns <code>False</code> value, <code>DBGrid</code> can not define current record position and it shows vertical scrollbar in three positions. <code>DBGridEh</code> component has possibility to show proportional scrollbar for no sequenced dataset. To do it we need to activate SumList and create list of record bookmars. Set <code>SumList.Active</code> to <code>True</code> and <code>SumList.VirtualRecords</code> to <code>True</code>. <code>SumList</code> will run through dataset and create list of records bookmarks. If you use client/sever technology to access database <code>SumList</code> will force dataset to fetch all records, but this operation can take much time. Keep in mind that <code>VirtualRecords</code> will work only for full relationship bookmarks dataset, it means that <code>DataSet.ComapreBookmark</code> function has to return &gt; 0 if <code>record1</code> is after <code>record2</code> (i.e. record to which <code>bookmark1</code> indicates has to be after record to which <code>bookmark2</code> indicates). It must return <code>0</code> if <code>bookmark1</code> and <code>bookmark2</code> points to the same record, and value <code>&lt; 0</code> if record1 is before <code>record2</code>. <code>TBDEDataSet</code> in most cases support full relationship bookmarks.</p><p>You can control the horizontal and vertical scrolling bar using the <code>Grid.VertScrollBar</code> and <code>Grid.HorzScrollBar</code> properties of <code>TGridScrollBarEh</code> type. The <code>TGridScrollBarEh</code> class contains the following properties:</p><dd><p><code>Tracking: Boolean</code> Specifies whether or not the position in the grid to change immediately when user change the position of the slider when the user clicked the mouse and drags the slider.</p><p><code>Visible: Boolean</code> Specifies permanent visibility slider bar. (The property is out of date, use VisibleMode)</p><p><code>SmoothStep: Boolean</code> Sets the scroll step of the grid in which the grid is not specified on the border of the cell, and pixel-by-pixel. When SmoothStep, the grid visually scroll more smoothly, with the end of the scroll bar does not guarantee that the left-top cell of the grid will start at the border of the visible area of the grid. SmoothStep mode only works when the grid is bind to the DataSet of TMemTableEh type.</p><p><code>VisibleMode: TScrollBarVisibleModeEh</code> Sets the scrollbar show mode:</p><dl><dd><p><code>sbAlwaysShowEh</code> - the scroll bar is always displayed.</p><p><code>sbNeverShowEh</code> - scroll bar is always hidden.</p><p><code>sbAutoShowEh</code> - the scroll bar is displayed only if the grid is not reduced in the visible area of the client window.</p></dd></dl><p><code>Height: Integer</code> For the horizontal scroll bar sets the height of the strip. For vertical property is not available.</p><p><code>Width: Integer</code> For the vertical scroll bar sets the width of the strip. For horizontal scroll feature is not available.</p><p><code>ExtraPanel: TDBGridEhScrollBarPanel</code> The property is available only for horizontal stripes. The property specifies the parameters of the additional panel that is displayed in the left part of the band. A detailed description, see the next section.</p><p>To customize scollbars you need to use subprtoperties of HorzScrollBar and VertScrollBar properties.</p><p><code>Tracking: Boolean</code> This property …</p><p><code>SmoothStep: Boolean</code> …</p><p><code>ExtScrollBar: TScrollBar;</code> …</p><p><code>Visible: Boolean;</code> …</p><p><code>VisibleMode: TScrollBarVisibleModeEh;</code><code>sbAlwaysShowEh</code> Always shows scrollbar.</p><p><code>sbNeverShowEh</code> Never shows scrollbar.</p><p><code>sbAutoShowEh</code> Shows or hides scrollbar automatically depending on visible area of grid.</p></dd><p>If <code>DBGridEh</code> is connected to <code>DataSet</code> of <code>TMemTableEh</code> type then on vertical scrolling grid doesn’t change position of active record.</p>',5),a=[d];function i(s,l){return o(),t("div",null,a)}const p=e(c,[["render",i],["__file","24-customizing-scrollbars.html.vue"]]);export{p as default};
