import{_ as e}from"./_plugin-vue_export-helper.cdc0426e.js";import{o,c as i,f as t}from"./app.ac77cf73.js";const a="/online-help-vuepress-hope/assets/DBGridEh.LookupComboBox.a82ddf91.png",d={},l=t('<h1 id="configure-the-lookup-relationship-in-grid-columns" tabindex="-1"><a class="header-anchor" href="#configure-the-lookup-relationship-in-grid-columns" aria-hidden="true">#</a> Configure the lookup relationship in grid columns</h1><h4 id="general-concepts-about-lookup-fields" tabindex="-1"><a class="header-anchor" href="#general-concepts-about-lookup-fields" aria-hidden="true">#</a> General concepts about lookup fields.</h4><p>Before describing the possiblites of the grid when working with lookup fields, lets consider what is a lookup field in terms of the standard <code>TField</code> class. Lookpup field – is a field of <code>DataSet</code> with a <code>TField</code> configured in such a way that it show text from the dictionary field basing on the code of a key field of dataset. The second DataSet serves as a dictionary. Typically, lookup fields are created at the Design-Time based on field DataSet selected from the database. When configuring fields in the <code>DataSet</code> the designer creates a Persistent field based on the selected fields from the database, and then adds calculated and lookup fields. To configure the lookup field, you can use a dialog to add a new field in <code>DataSet</code>.</p><p>![](../../../images/Lookup fields Dialog.png)</p><p>You can also fill TField properties in the Object Inspector as follows:</p><ul><li>Set <code>FieldKind = fkLookup</code></li><li>Set <code>KeyField</code> to the field name that contains the code values in the original DataSet</li><li>Set <code>LookupDataSet</code> to DataSet that contains dictionary values</li><li>Set <code>LookupKeyFields</code> to a field name that contains the code values in the dictionary DataSet</li><li>Set <code>LookupResultField</code> to a field name in dictionary DataSet whose content should be displayed instead of the code.</li></ul><h4 id="lookup-columns-in-the-grid" tabindex="-1"><a class="header-anchor" href="#lookup-columns-in-the-grid" aria-hidden="true">#</a> Lookup columns in the grid.</h4><p>DBGridEh and DBVertGridEh automatically determine then Field.FieldKind is lookup and create a cell with a dropdown list so that it looks like a LookupComboBox.</p><p>In addition, the column (<code>TColumnEh</code> and <code>TFieldRow</code>) contain additional properties for the empowerment of working with lookup fields.</p><p><img src="'+a+'" alt="" loading="lazy"></p><p>To display multiple fields in the drop-down list fill the Column.LookupDisplayFields property by the list of field names separated by a semicolon - <code>;</code>. By default, the drop-down list displays a single column with the value specified in the field <code>TField.LookupResultField</code>. You can customize the list so that it displays multiple fields from the DataSet specified property TField.LookupDataSet.</p><ul><li><p>Method 1: Fill <code>TColumnEh.DropDownBox.ListFieldNames</code> property by the list of fields. Several fields are separated by semicolons - <code>;</code>.</p></li><li><p>Method 2: Fill <code>TColumnEh.DropDownBox.Columns</code> collection with elements. With this method, you can also customize the font and the other a property of each column drop-down list.</p></li></ul><h4 id="configure-lookup-relationships-directly-into-the-grid-column" tabindex="-1"><a class="header-anchor" href="#configure-lookup-relationships-directly-into-the-grid-column" aria-hidden="true">#</a> Configure lookup relationships directly into the grid column.</h4><p><code>DBGridEh</code> and <code>DBVertGridEh</code> has the ability to create lookup relationships directly into grid columns. To do this, use the sub-properties of <code>TColumn.LookupParams</code> property. Fill in the sub-properties similar to how it is configured in the TField. In this case, leave TColumn.FieldName property empty. The <code>TColumn.LookupParams</code> property has the following subproperties: <br></p><p><code>property KeyFieldNames: String</code></p><dd>Defines a field (or fields) in Grid.DataSource.DataSet that specifies the value to search LookupDataSet. Several fields are separated by &#39;;&#39;.</dd><br><p><code>property LookupCache: Boolean</code></p><dd>Specifies whether to copy the key values in the internal buffer to speed up the operation of drawing a grid of cells.</dd><br><p><code>property LookupDataSet: TDataSet</code></p><dd>Specifies the dataset record values to be displayed in the lookup control instead of the field KeyField</dd><br><p><code>property LookupDisplayFieldName: String</code></p><dd>Specify the field whose value will be displayed on the screen.</dd><br><p><code>property LookupKeyFieldNames: String</code></p><dd>Defines a field (or fields) in LookupDataSet value will be used to search records for the value of the field specified by KeyFieldNames property.</dd><br><p>With a large number of customized lookup columns in the grid, drawing data may be slow because Search lookup values occurs in each cell drawing. But drawing can be accelerated, if you use TMemTableEh as a DataSet. MemTableEh provides the ability to create in DBGridEh associated buffer values found and dynamically update it as a data changes in MemTableEh.</p>',30),s=[l];function n(r,c){return o(),i("div",null,s)}const u=e(d,[["render",n],["__file","04-configure-the-lookup-relationship-in-grid-columns.html.vue"]]);export{u as default};
